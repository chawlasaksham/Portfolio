import React from "react";
import Hero from "../components/hero"; // Ensure correct import

const Home = () => {
  return (
    <div>
      <Hero />
    </div>
  );
};

export default Home;
